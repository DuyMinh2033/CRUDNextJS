import React from "react";
import { Button } from "react-bootstrap";

const page = () => {
  return (
    <div className="w-10">
      Welcome Facebook
      <Button variant="primary" className="m-2 flex justify-center ">
        Hello
      </Button>
    </div>
  );
};

export default page;
